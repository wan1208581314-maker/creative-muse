# 创意发散 - 项目日志

## v2.3 — 温度控制滑块（2026-05-09）

### 新增功能

**联想温度控制：**
- 输入框下方添加"集中 ↔ 发散"滑块（0~1，默认 0.7）
- 低温（0~0.3）：联想词更贴近原词、更常规
- 高温（0.7~1.0）：联想词更跳跃、更有意外感
- 滑块实时显示当前数值
- 暗色模式适配

**改动文件：**
- `src/input.js` — 添加滑块 UI，返回 `getTemperature()` 方法
- `src/main.js` — 传递温度参数给 `fetchAssociations`，版本号更新为 v2.3
- `src/api.js` — `fetchAssociations` 接受 temperature 参数
- `server/index.js` — 从请求体读取 temperature，传给 DeepSeek
- `src/style.css` — 滑块样式（黄色滑块、居中布局、暗色模式）

## v2.2 — 弹簧物理与飘逸运动（2026-05-09）

### 新增功能

**弹簧物理拖拽（需求 #24）：**
- 拖拽父节点时，所有后代节点跟随运动
- 采用 lerp + 多频飘动方案，节点有"被风吹过"的飘逸感
- 每个节点独立参数：跟随速度、飘动频率、飘动幅度、相位各不相同
- 层级越深的节点飘动幅度越大、跟随越慢
- 使用 `transform: translate()` 移动节点，避免与 CSS 浮动动画冲突
- 拖拽结束后 `commitSpring()` 将最终位置写回数据模型

**关键实现细节：**
- `startSpring()` — 初始化时为每个后代节点生成独立的飘动参数（随机相位、频率、幅度）
- `updateSpring()` — 每次 mousemove 调用，lerp 跟随 + 三层 sin/cos 飘动叠加
- `commitSpring()` — mouseup 时清除 transform，用 left/top 定位
- `clearSpring()` — 清理弹簧状态，恢复动画

**CSS 处理：**
- `.spring-active` 类：`animation: none !important` 抑制 CSS 浮动动画
- 弹簧期间通过 `el.style.animation = 'none'` 内联样式强制禁用

### Bug 修复

- **折叠/展开 UI 缺失（需求 #25）：** 节点数量徽章改为可点击，点击切换折叠/展开状态。折叠时徽章显示"N ▸"灰色样式
- **撤销选中状态不同步：** `undo()` 中添加 `removeConfirmIcon()` 调用，子节点全部撤销时重置父节点 collapsed 状态
- **节点入场定位闪烁：** 使用固定 inline width/height（120px/90px）代替 getBoundingClientRect，避免动画期间尺寸错误

### Prompt 优化

**联想接口（/api/associate）：**
- System Prompt：从泛泛的"创意联想助手"改为强调强相关性
- User Prompt：8 个联想方向建议（工具/场景/上下游/风格/人物/痛点/搭配/趋势）
- 核心原则：每个词必须与输入词强相关，"看到就能理解为什么想到它"
- temperature: 0.7（比之前更聚焦）

**创意方案接口（/api/generate）：**
- 强调可落地产出：标题、核心概念、执行步骤、平台建议、目标人群
- temperature: 1.0（保持发散性）

### 改动文件清单

| 文件 | 改动内容 |
|------|----------|
| `src/graph.js` | 弹簧物理系统（startSpring/updateSpring/commitSpring/clearSpring）、折叠/展开 toggleCollapse、徽章点击事件、撤销修复 |
| `src/style.css` | .spring-active 样式、.node-badge 可点击、.collapsed 状态、徽章 cursor |
| `src/main.js` | updateLatestHistory 传入 currentWord 做验证、history 回调中调用 inputApi.dock() |
| `src/input.js` | 返回 `{ dock }` 对象暴露 dock 方法 |
| `src/history.js` | updateLatestHistory 增加 word 参数验证 |
| `server/index.js` | 联想/创意两个接口的 prompt 全面重写、callDeepSeek 支持 temperature 参数 |
| `创意发散-项目指南.md` | 更新版本记录 |

---

## v2.1 — Prompt 优化（2026-05-08）

### 改动

- 联想 Prompt：从"只返回JSON数组"改为"潮流创意策划师"角色，5 条具体要求
- 创意方案 Prompt：新增 slogan 标题、核心概念、执行步骤、平台建议、目标人群
- temperature 调整

---

## v2.0 — 全面改进（2026-05-08）

### 新增功能

- 画布平移缩放（CSS transform + screenToWorld）
- 暗色模式（CSS 变量 + localStorage）
- 节点折叠/展开（递归隐藏 + 数量徽章）
- 画布控件（放大/缩小/适应/清空 + 缩放百分比）
- 空白欢迎状态
- 响应式适配（<600px）

### Bug 修复

- 节点首次渲染位置闪烁
- SVG 连线 resize 后错乱
- DeepSeek JSON 解析兼容 markdown 包裹

### 视觉改进

- SVG 贝塞尔曲线连线
- 新节点入场动画
- 暗色模式全元素适配

---

## v1.0 — 基础版本

- 词语输入 → DeepSeek 联想 → 节点图谱
- 点击节点展开联想词
- 右键选择词语 → 生成创意方案
- 历史记录（localStorage）
- 玻璃态 UI
